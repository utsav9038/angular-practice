import { RedcolorsDirective } from './redcolors.directive';

describe('RedcolorsDirective', () => {
  it('should create an instance', () => {
    const directive = new RedcolorsDirective();
    expect(directive).toBeTruthy();
  });
});
